import 'package:flutter/material.dart';

class CronometroButton extends StatelessWidget {
  const CronometroButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(
      child: child,
      onPressed: () {},
    );
  }
}
